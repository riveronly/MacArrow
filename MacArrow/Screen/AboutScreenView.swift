import SwiftUI

struct AboutScreenView: View {
    var body: some View {
        Form {
            Section{
                Text("MacArrow is mouse helper, who can make mouse more useful!")
            }
        }
        .padding()
    }
}
